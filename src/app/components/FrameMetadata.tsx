"use client";

/**
 * This component is now deprecated. We are using direct meta tags in layout.tsx instead.
 * Keeping this file for reference but it's not being used.
 */
export default function FrameMetadata() {
  // This component is no longer used, direct meta tags are added in layout.tsx
  return null;
}
